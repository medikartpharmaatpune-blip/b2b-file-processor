apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-processor-svc
spec:
  selector:
    app: {{ .Release.Name }}-processor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
